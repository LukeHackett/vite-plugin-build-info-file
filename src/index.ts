export type { BuildInfoFilePluginConfig, ContributorConfig, GitContributorConfig, Contributor } from './types';

// eslint-disable-next-line import/no-default-export
export { buildInfoFile as default } from './plugin';
